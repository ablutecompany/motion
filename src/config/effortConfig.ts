export const EffortConfig = {
  effortTargetScore: 1500, // Pontuação canónica esperada para 1 sessão
  beastThreshold: 1.0,     // Apenas se score / target > 1.0 é que engatilha Beast Mode
  scoringVersion: '1.0'
};
